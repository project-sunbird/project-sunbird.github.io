definitions:
  RegisterOrgResponse:
    description: The response of Sunbird API containing a registered organisation
    type: object
    properties:
      id:
        type: string
      addType:
        type: string
      addressLine1:
        type: string
      addressLine2:
        type: string
      city:
        type: string
      state:
        type: string	
      zipcode:
        type: string
      country:
        type: string
      updatedBy:
        type: string
      updatedDate:
         type: int
         format: "YYYY-MM-DD HH:mm:ss:nn"
      createdDate:
  	 type: int
	 format: "YYYY-MM-DD HH:mm:ss:nn"
      isDeleted:
          type: boolean
      createdBy:
          type: string
      userId:
          type: string	  
     
     

