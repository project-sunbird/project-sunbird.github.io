definitions:
  AddressResponse:
    description: Response of Sunbird API containing a address
    type: object
    properties:
      id:
        type: string
      addType:
        type: string
      addressLine1:
        type: string
      addressLine2:
        type: string
      city:
        type: string
      state:
        type: string	
      zipcode:
        type: string
      country:
        type: string
      updatedBy:
        type: string
      updatedDate:
         type: string
         format: yyyy-MM-dd HH:mm:ss:SSSZZZZ
      createdDate:
  	 type: string
	 format: yyyy-MM-dd HH:mm:ss:SSSZZZZ
      isDeleted:
          type: boolean
      createdBy:
          type: string
      userId:
          type: string	  
     
     
